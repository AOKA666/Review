# PROJECT

Review：用于记录每日红榜/黑榜、深度复盘和 AI 周复盘的 Next.js 应用。

# TODO

- [x] 完成首次本地安装、构建和开发服务验证；本地 Supabase 数据接口可用
- [x] 周复盘严格只渲染红榜、黑榜、关键规律、下周行动四张卡片，忽略模型返回的标题和前言
- [x] 历史记录默认只展开当月，后续同步到的历史月份自动折叠
- [x] 历史月份列表限制高度并支持独立纵向滚动
- [x] 周复盘按周次和语言持久化，刷新或换设备后读取已有内容，只有手动“重新生成”才覆盖
- [x] Obsidian 文件夹句柄持久化后，后续授权直接复用已选文件夹，不再重复弹出文件夹选择器
- [x] 已保存的 Obsidian 文件夹仍有写权限时直接复用，不再重复调用授权请求
- [x] Obsidian 授权后仍可通过独立入口重新选择同步文件夹
- [x] Obsidian 改为仅手动同步，输入自动保存不再写本地 Vault，避免反复唤醒外部监听程序或黑窗口；异常路径完整捕获
- [x] Obsidian 同步按钮与状态分离；同步成功显示提示和“已同步”状态，后续编辑自动变为“有更新待同步”
- [x] Supabase 云端保存失败时，即使浏览器缓存写入成功也明确提示“保存失败”
- [x] 同日期本地缓存与数据库 ID 不一致时复用数据库 journal ID，避免修改被外键引用的主键导致保存失败
- [ ] 确认 Vercel 正式环境变量配置与本地 `.env.local` 保持一致
- [ ] 按 `调整建议.md` 的方向，把首页重构成“轻记录 + 可选深度复盘”双层结构（默认只展开顶部 textarea）
- [ ] 清理仓库根目录的调试脚本 `inspect.py`、`replace2.py`，判断是否还需要保留
- [ ] 评估将周复盘从现有独立 JSONB 文档迁移到结构化 `weekly_summaries` 表

# DECISIONS

- 周复盘前端解析器只将四类业务小标题识别为卡片；模型返回的文档标题、日期范围和标题前言不单独生成卡片。
- 月份折叠状态与已知月份放在同一个 React state 中，由纯函数一次性协调；避免 Strict Mode 重放 state updater 时因修改外部 ref 导致历史月份全部展开。
- 根布局允许浏览器扩展在 hydration 前给 `<html>` 注入属性，避免沉浸式翻译等扩展触发无实际 UI 差异的 hydration 告警。
- 历史月份区域使用视口相关最大高度和独立滚动条，避免展开多个月份后拉长整个页面。
- 周复盘以 `周结束日期 + 语言` 作为缓存键，保存在 Supabase `repano_reviews` 的独立 `weekly_summaries` 文档中；服务端优先返回已保存内容，明确重新生成时才调用模型并覆盖。
- Obsidian 文件夹句柄保存在浏览器 IndexedDB；权限需要恢复时复用原句柄请求写权限，只有首次连接或主动“更换 Obsidian 文件夹”才打开文件夹选择器。
- Obsidian 同步优先检查已保存文件夹的现有写权限；权限仍有效时直接同步，只有浏览器确实撤销权限后才重新请求授权。
- “立即同步 Obsidian”复用当前文件夹；“更换 Obsidian 文件夹”始终打开目录选择器并保存新文件夹句柄。
- Obsidian 操作按钮保持中性样式，旁边独立显示“未连接 / 有更新待同步 / 正在同步 / 已同步 / 失败”等状态；成功后显示短时提示，已同步内容发生编辑后立即回到“有更新待同步”。
- Review 自动保存只写浏览器缓存和 Supabase；Obsidian Vault 仅通过“立即同步”手动写入，避免每次输入保存都唤醒本地文件监听器。Obsidian 的权限检查、句柄恢复、授权和写入异常全部在调用链内捕获，只更新页面状态，不输出会触发开发错误浮层的控制台错误或警告。
- 保存成功状态以 Supabase 云端写入成功为必要条件；浏览器缓存仅作为本地副本，云端失败时界面必须显示“保存失败”。
- 保存 journals 前先按日期读取数据库中的规范 ID；同日期已有记录时不得用本地缓存 ID 覆盖数据库主键。保存接口只向前端返回安全的失败阶段标识，完整数据库错误保留在服务端日志。

# LOG
- 2026-08-27：顶部 Obsidian 同步、更换文件夹和 Markdown 导出保持原有极简按钮布局，仅增加轻量线性图标。

- 2026-07-14 仓库从 `https://github.com/AOKA666/Review.git` 克隆到本地 `Review/` 工作区，确认 main 分支最新提交为 `周总结`（76d1724，2026-07-13）。
- 2026-07-14 修复 `# 周复盘（日期范围）` 被解析成第五张“周复盘总结”卡片的问题；新增解析器回归测试，测试和生产构建通过。
- 2026-07-15 完成历史月份默认折叠、历史列表独立滚动和周复盘持久化；新增回归测试，5 项测试及生产构建通过。
- 2026-07-15 修复 React Strict Mode 重放 updater 导致所有月份展开的问题，并抑制浏览器翻译扩展注入 `<html>` 属性造成的 hydration 告警；7 项相关测试及生产构建通过。
- 2026-08-13 修复每次打开都重新选择 Obsidian 文件夹的问题；新增持久句柄复用回归测试，相关 9 项测试与生产构建通过。
- 2026-08-16 修复本机保存成功但 Supabase 同步异常时误报“保存失败”的问题；已处理的云同步异常不再触发开发错误浮层；新增 2 项回归测试并通过，生产构建通过。
- 2026-08-19 调整保存状态判定：浏览器缓存成功但 Supabase 云端保存失败时，前端明确显示“保存失败”，避免把仅本地缓存误报为完整保存成功。
- 2026-08-22 保留输入停止 1 秒后的 Obsidian 自动同步，将已捕获的写入异常从错误日志调整为后台告警，避免 Next.js 开发错误浮层快速闪现；保留顶部立即同步按钮并新增回归测试。
- 2026-08-22 修复已保存 Obsidian 文件夹仍有写权限时也重复调用授权的问题；增加“权限有效则跳过授权请求”回归测试。
- 2026-08-22 恢复授权后的“更换 Obsidian 文件夹”独立入口，避免立即同步操作复用旧目录后无法重新选择目标文件夹。
- 2026-08-22 修复本地缓存与数据库同日期记录 ID 分叉后，journals upsert 修改被外键引用主键而持续保存失败的问题；新增规范 ID 复用测试和分阶段错误标识。
- 2026-08-26 继续修复 Obsidian 同步黑窗口：此前每次输入自动保存都会写 Vault，可能反复唤醒本地文件监听器；现改为只保留“立即同步”手动写入。权限检查也已纳入完整异常捕获，并移除句柄恢复、立即同步和更换文件夹路径中的浮层触发型控制台日志。
- 2026-08-27 完善 Obsidian 同步反馈：按钮不再用常亮颜色表达状态，新增独立状态徽标；同步成功显示绿色提示，之后编辑当前记录会将状态更新为“有更新待同步”。相关 10 项测试及生产构建通过。
